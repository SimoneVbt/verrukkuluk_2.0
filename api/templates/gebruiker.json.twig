{
    "id": 1,
    "remote_id": {{ user.id }},
    "gebruikersnaam": "{{ user.username }}",
    "email": "{{ user.email }}",
    {% if user.afbeelding is defined %}"afbeelding": "{{ user.afbeelding }}",{% endif %}
    "wachtwoord": "{{ user.password }}"
}
