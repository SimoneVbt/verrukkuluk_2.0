{
    "id": 1,
    "remote_id": {{ user.id }},
    "gebruikersnaam": "{{ user.username }}",
    "email": "{{ user.email }}",
    {% if user.foto is defined %}"foto": "{{ user.afbeelding }}",{% endif %}
    "wachtwoord": "{{ user.password }}"
}
