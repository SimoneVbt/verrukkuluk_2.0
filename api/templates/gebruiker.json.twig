{
    "id": 1,
    "remote_id": {{ user.id }},
    "gebruikersnaam": "{{ user.username }}",
    "email": "{{ user.email }}",
    {% if user.afbeelding is not empty %}
        "afbeelding": "{{ user.afbeelding }}",
    {% endif %}
    "wachtwoord": "{{ user.password }}"
}
