[
    {% for kt_ in kt %}
    {
        "id": {{ kt_.id }},
        "record_type": "{{ kt_.recordType|e('js')|raw }}",
        "omschrijving": "{{ kt_.omschrijving|e('js')|raw }}"
    {{ kt_ == kt|last ? "}" : "}," }}
    {% endfor %}
]